Use the write tool to create /workspace/app.py containing a minimal Flask TODO REST API with GET /todos and POST /todos in memory. Then use the write tool to create /workspace/requirements.txt containing exactly Flask==3.0.0. Do not do anything else.
